[project]
name = "stormm-modal"
version = "0.1.0"
description = "Modal GPU harness validating explicit-solvent (PME) molecular dynamics in Psivant/stormm"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "modal>=1.5.5",
]

[tool.uv]
package = false
